const DIALOG_CLASS='sdf-nova-dialog';
function skinTestDialog(app:any,fallback?:HTMLElement){const root=(app?.element instanceof HTMLElement?app.element:fallback)??undefined;if(!root)return;root.classList.add(DIALOG_CLASS);const form=root.querySelector('form')??root.querySelector('.window-content');if(!form||form.querySelector('.sdf-dialog-tag'))return;const tag=document.createElement('div');tag.className='sdf-dialog-tag';tag.innerHTML=`<i class="fas fa-dice-d6"></i> <span>${game.i18n.localize('SDF.Dialog.Console')}</span><i class="sdf-dialog-rule"></i>`;form.prepend(tag)}
export function installDialogSkin(){Hooks.on('renderTestDialog',(app:any,element:any)=>{try{skinTestDialog(app,element instanceof HTMLElement?element:element?.[0])}catch(error){console.warn('sr5-dice-flow | Dialog skin failed',error)}})}
