import{beforeAll,describe,expect,it}from'vitest';import{createTransaction}from'../src/core/transaction';import{isSecretRollMode,sanitizeRoll}from'../src/foundry/security';
beforeAll(()=>{(globalThis as any).game={i18n:{localize:(key:string)=>key,format:(key:string,data:any)=>`${key} ${data.revision}`}}});
describe('card details and privacy',()=>{it('recognizes Foundry and legacy private roll modes',()=>{expect(['gm','blind','gmroll','blindroll'].every(isSecretRollMode)).toBe(true);expect(isSecretRollMode('public')).toBe(false);expect(sanitizeRoll({toJSON:()=>({dice:'secret'})},'blind')).toBeUndefined()});it('renders SR5 card classes and pool details',async()=>{const{renderCard}=await import('../src/foundry/card');const tx=createTransaction({kind:'SkillTest',title:'Sneaking',basePool:8,pool:10,hits:4,limit:5,threshold:3,modifiers:[{id:'m',label:'Specialization',value:2,source:'Actor'}]},'u');const html=renderCard(tx);expect(html).toContain('sr5 chat-card roll-card sr5-roll-card-v2 sr5-dice-flow');expect(html).toContain('Specialization');expect(html).toContain('SDF.DicePool');expect(html).toContain('>10<')})});
